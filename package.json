{
  "name": "maintainiq-hackathon",
  "private": true,
  "scripts": {
    "build": "npm install --prefix MaintainIQ-Main --no-audit --no-fund && npm --prefix MaintainIQ-Main run build",
    "dev": "npm --prefix MaintainIQ-Main run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
